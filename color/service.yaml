apiVersion: v1
kind: Service
metadata:
  labels:
    app: color
  name: color
  namespace: color
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: color
status:
  loadBalancer: {}
