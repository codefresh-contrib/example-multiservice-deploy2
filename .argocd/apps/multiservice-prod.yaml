apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: multiservice-prod
  namespace: captest  # All app resources live in the argocd namespace
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    namespace: multiservice-prod
    name: in-cluster
  project: example-multiservice
  source:
    repoURL: https://github.com/codefresh-contrib/example-multiservice-deploy2.git
    targetRevision: main
    path: prod
    helm:
      parameters:
        - name: buildID
          value: argocd
      valueFiles:
        - values.yaml